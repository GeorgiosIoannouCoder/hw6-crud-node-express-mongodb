<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="description" content="CRUD|Node|Express|MongoDB" />
    <meta name="keywords" content="crud-node-express-mongodb" />
    <meta name="author" content="Georgios Ioannou" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CRUD|Node|Express|MongoDB</title>
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1 class="title">May Node and Express be with you.</h1>

    <p align="center">
      <img
        src="/favicon.png"
        alt="Yoda Star Wars character"
        width="100"
        height="100"
      />
      <img
        src="/darth_vader.png"
        alt="Darth Vader Star Wars character"
        width="100"
        height="100"
      />
    </p>

    <div class="row">
      <div class="container">
        <h2 class="header">Add quote</h2>
        <form action="/quotes" method="POST">
          <input type="text" placeholder="name" name="name" />
          <input type="text" placeholder="quote" name="quote" />
          <button type="submit">Submit</button>
        </form>
      </div>

      <div class="container">
        <h2 class="header">Darth Vader invades!</h2>
        <p>Replace first Yoda's quote with a quote written by Darth Vader</p>
        <button id="update-button">Replace Yoda's quote</button>
      </div>

      <div class="container">
        <h2 class="header">Remove Darth Vader!</h2>
        <p>
          Delete one Darth Vader's quote. Does nothing if there are no more
          Darth Vader's quote
        </p>
        <button id="delete-button">Delete Darth Vader's quote</button>
      </div>
    </div>

    <div class="container-quotes">
      <h2 class="header">Quotes</h2>
      <ul class="quotes">
        <!-- Loop through quotes -->
        <% for(var i = 0; i < quotes.length; i++) {%>
        <li class="quote">
          <!-- Output name from the iterated quote object -->
          <span><%= quotes[i].name %></span>:
          <!-- Output quote from the iterated quote object -->
          <span><%= quotes[i].quote %></span>
        </li>
        <% } %>
      </ul>
    </div>

    <div id="message"></div>

    <script src="/main.js"></script>
  </body>
</html>
